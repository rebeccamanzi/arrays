// retorna o 1º indice em que um elemento pode ser encontrado no array

const arr = [1,2,3,3,4,3];

const firstIndexOfItem = arr.indexOf(3);

console.log(firstIndexOfItem);
// 1 (índice do primeiro elemento que aparece no array com valor 3)