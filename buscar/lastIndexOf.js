// retorna o ÚLTIMO indice em que um elemento pode ser encontrado no array

const arr = [1,2,3,3,4,3];

const lastIndexOfItem = arr.lastIndexOf(3);

console.log(lastIndexOfItem);
// 5 (índice do ÚLTIMO elemento que aparece no array com valor 3)